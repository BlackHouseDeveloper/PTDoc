@page "/patients"
@attribute [Authorize]
@using PTDoc.Application.Services
@using PTDoc.Application.DTOs
@using PTDoc.UI.Components.Patients.Models
@inject NavigationManager NavigationManager
@inject IPatientService PatientService
@inject IToastService ToastService
@rendermode InteractiveServer
@implements IDisposable

<PageTitle>Patients - PTDoc</PageTitle>

<div class="patients-page">
    <GlobalPageHeader OnPrimaryAction="OpenAddPatientModal" />

    <div class="patients-page-body">
        <div class="patients-page-search">
            <PatientSearchInput 
                Value="@searchQuery"
                ValueChanged="@HandleSearchChanged"
                Placeholder="Search by name or DOB" 
            />
        </div>

        <div class="patients-page-content">
            @if (isLoading)
            {
                <div class="patients-page-loading">
                    @for (var i = 0; i < 6; i++)
                    {
                        <LoadingSkeleton Variant="SkeletonVariant.Rectangle" Size="SkeletonSize.Large" />
                    }
                </div>
            }
            else
            {
                <PatientCardSection 
                    Items="@GetFilteredPatients()" 
                    OnCardClick="@HandlePatientClick" 
                />
            }
        </div>
    </div>
</div>

<AddPatientModal 
    @bind-IsOpen="isAddPatientModalOpen"
    OnSubmit="@HandleAddPatient"
/>

@code {
    private bool isLoading = false;
    private bool isAddPatientModalOpen = false;
    private string searchQuery = string.Empty;
    private List<PatientListItemVm> patients = new();
    private CancellationTokenSource? _searchCts;

    protected override async Task OnInitializedAsync()
    {
        await LoadPatientsAsync();
    }

    private async Task LoadPatientsAsync(string? query = null)
    {
        isLoading = true;
        StateHasChanged();
        try
        {
            var results = await PatientService.SearchAsync(query: query, take: 200);
            patients = results.Select(ToVm).ToList();
        }
        catch (Exception ex)
        {
            Console.WriteLine($"[Patients] Error loading patients: {ex.Message}");
            ToastService.ShowError("Failed to load patients.");
        }
        finally
        {
            isLoading = false;
            StateHasChanged();
        }
    }

    private async Task HandleSearchChanged(string value)
    {
        searchQuery = value;

        _searchCts?.Cancel();
        _searchCts?.Dispose();
        _searchCts = new CancellationTokenSource();
        var token = _searchCts.Token;

        try
        {
            await Task.Delay(300, token);
            await LoadPatientsAsync(string.IsNullOrWhiteSpace(value) ? null : value);
        }
        catch (TaskCanceledException)
        {
            // Superseded by a newer keystroke — ignore.
        }
    }

    private IReadOnlyList<PatientListItemVm> GetFilteredPatients() => patients;

    private void OpenAddPatientModal()
    {
        isAddPatientModalOpen = true;
    }

    private async Task HandleAddPatient(AddPatientModal.PatientFormData formData)
    {
        try
        {
            var request = new PTDoc.Application.DTOs.CreatePatientRequest
            {
                FirstName = formData.FirstName,
                LastName = formData.LastName,
                DateOfBirth = formData.DateOfBirth ?? DateTime.Today,
                Email = formData.Email,
                Phone = formData.PhoneNumber
            };
            await PatientService.CreateAsync(request);
            ToastService.ShowSuccess($"{formData.FirstName} {formData.LastName} added.");
            await LoadPatientsAsync(string.IsNullOrWhiteSpace(searchQuery) ? null : searchQuery);
        }
        catch (Exception ex)
        {
            Console.WriteLine($"[Patients] Error creating patient: {ex.Message}");
            ToastService.ShowError("Failed to add patient. Please try again.");
        }
    }

    private void HandlePatientClick(PatientListItemVm patient)
    {
        NavigationManager.NavigateTo($"/patient/{patient.Id}");
    }

    private static PatientListItemVm ToVm(PatientListItemResponse p) => new()
    {
        Id = p.Id.ToString(),
        DisplayName = p.DisplayName,
        DateOfBirth = p.DateOfBirth == default ? null : p.DateOfBirth.ToString("MMM d, yyyy"),
        StatusLabel = p.IsArchived ? "Inactive" : "Active",
        StatusVariant = p.IsArchived ? BadgeVariant.Default : BadgeVariant.Success
    };

    public void Dispose()
    {
        _searchCts?.Cancel();
        _searchCts?.Dispose();
    }
}
