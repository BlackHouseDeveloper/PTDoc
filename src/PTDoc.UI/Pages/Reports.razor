@page "/reports"
@attribute [Authorize(Policy = "ClinicalStaff")]
@inject NavigationManager Navigation

<PageTitle>Reports - PTDoc</PageTitle>

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/export-center?template=progress-summary", replace: true);
    }
}
