{
  "name": "ptdoc-web-ui-qa",
  "version": "0.0.0",
  "private": true,
  "description": "Responsive and theme QA checks for the PTDoc Web UI.",
  "scripts": {
    "install:browsers": "playwright install chromium",
    "test": "playwright test",
    "test:audit-remediation": "playwright test tests/audit-remediation.spec.ts",
    "test:beta-e2e": "playwright test tests/beta-e2e.spec.ts",
    "test:patient-documents": "playwright test tests/patient-documents.spec.ts",
    "test:responsive": "playwright test tests/responsive.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0"
  }
}
