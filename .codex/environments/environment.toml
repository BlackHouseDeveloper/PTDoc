# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "PTDoc"

[setup]
script = ""

[setup.darwin]
script = '''
set -euo pipefail

command -v dotnet >/dev/null
command -v npm >/dev/null

actual_sdk="$(dotnet --version)"
required_sdk="$(sed -nE 's/.*"version"[[:space:]]*:[[:space:]]*"([^"]+)".*/\1/p' global.json)"

if [[ "$actual_sdk" != "$required_sdk" ]]; then
  echo "PTDoc requires .NET SDK $required_sdk; found $actual_sdk." >&2
  exit 1
fi

dotnet restore PTDoc.sln
npm --prefix tests/PTDoc.Web.UiQa ci'''
